export type CreateUsersParams = {
    username: string;
    password: string;
}

export type UserProfileParams = {
    firstName: string;
    lastName: string; 
    age: number;
    dob: string; 
}

export type PostParams = {
    title: string;
    description: string; 
}